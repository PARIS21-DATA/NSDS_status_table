# **Africa**

```{r,  echo=FALSE}
nsds_africa %>% 
  mutate(Status = ifelse(Status == "Expired", cell_spec("Expired", bold = T, color = "white", background = "red"), 
                         ifelse(Status == "Implementation", cell_spec("Implementation", bold = T, color = "white", background = "green"),Status))) %>%
  knitr::kable("html", escape = FALSE, booktabs = TRUE) %>% 
  kable_styling(full_width = F, bootstrap_options = c("hover", "condensed")) %>% 
  #mutate_all(~cell_spec(.x, background = case_when(.x == "Expired" ~ "red")
  #cell_spec(which(nsds_africa$Status == "Expired"), bold = T, color = "white", background = "red") %>%
  scroll_box(width = "800px", height = "1200px")
```